<Window
    x:Class="WuPilot.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WuPilot">

    <Grid x:Name="RootGrid" Loaded="RootGrid_Loaded" KeyDown="RootGrid_KeyDown" IsTabStop="True"
          Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
            <RowDefinition Height="54" />
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Grid.Row="0" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="48" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <FontIcon Grid.Column="0" Glyph="&#xE895;" FontSize="18" HorizontalAlignment="Center" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <TextBlock Text="WuPilot" FontSize="16" FontWeight="SemiBold" />
                <TextBlock Text="Windows Update Workbench" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <StackPanel Grid.Column="2" Margin="8,0,144,0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button x:Name="CompletionButton" Content="Notices" Click="CompletionButton_Click" ToolTipService.ToolTip="Recent completed and failed operations">
                    <Button.Flyout>
                        <Flyout x:Name="CompletionFlyout">
                            <StackPanel Width="430" MaxHeight="520" Spacing="8">
                                <Grid><TextBlock Text="Completion center" FontSize="18" FontWeight="SemiBold" /><Button Content="Clear all" HorizontalAlignment="Right" Click="ClearCompletionNotices_Click" /></Grid>
                                <TextBlock x:Name="CompletionEmptyText" Text="No recent operation notices." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <ListView x:Name="CompletionNoticeList" ItemsSource="{x:Bind CompletionNotices}">
                                    <ListView.ItemTemplate><DataTemplate><Grid Padding="6" ColumnSpacing="8"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{Binding Title}" FontWeight="SemiBold" /><TextBlock Text="{Binding Summary}" TextWrapping="Wrap" FontSize="11" /><TextBlock Text="{Binding Severity}" Foreground="{StaticResource WuPilotAccentBrush}" FontSize="11" /></StackPanel><StackPanel Grid.Column="1"><Button Content="Open" Tag="{Binding}" Click="OpenCompletionNotice_Click" /><Button Content="Dismiss" Tag="{Binding}" Click="DismissCompletionNotice_Click" /></StackPanel></Grid></DataTemplate></ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                </Button>
                <Border Padding="10,4" CornerRadius="12" Background="{ThemeResource SystemFillColorCautionBackgroundBrush}">
                    <TextBlock x:Name="ElevationBadgeText" Text="Checking access…" FontSize="12" />
                </Border>
            </StackPanel>
        </Grid>

        <NavigationView x:Name="Navigation" Grid.Row="1" PaneDisplayMode="LeftCompact" IsBackButtonVisible="Collapsed" IsSettingsVisible="False"
                        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" SelectionChanged="Navigation_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="ScanNav" Content="Scan and review" Tag="scan" Icon="Find" IsSelected="True" />
                <NavigationViewItem x:Name="CompareNav" Content="Compare scans" Tag="compare">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE8AB;" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Name="ControlsNav" Content="Update controls" Tag="controls">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE713;" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Name="PerformanceNav" Content="Performance" Tag="performance">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE9D2;" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Watchlist" Tag="watchlist">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE734;" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Registered sources" Tag="sources">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE774;" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Diagnostics" Tag="diagnostics">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE9D9;" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Update history" Tag="history" Icon="Clock" />
                <NavigationViewItem Content="Activity" Tag="activity">
                    <NavigationViewItem.Icon><FontIcon Glyph="&#xE9D5;" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="About" Tag="about" Icon="Help" />
            </NavigationView.MenuItems>

            <Grid>
                <Grid x:Name="ScanView" Padding="24,16,24,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Scan and review" Style="{StaticResource TitleTextBlockStyle}" />
                            <TextBlock Text="Compare applicable updates across policy-managed and direct Microsoft sources." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <Button Grid.Column="1" Click="Export_Click" IsEnabled="{x:Bind HasScanReport, Mode=OneWay}" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" Spacing="8"><FontIcon Glyph="&#xEDE1;" FontSize="14" /><TextBlock Text="Export evidence" /></StackPanel>
                        </Button>
                    </Grid>

                    <Border Grid.Row="1" Style="{StaticResource SectionCardStyle}" Margin="0,0,0,16">
                        <Grid ColumnSpacing="20">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="2*" /><ColumnDefinition Width="1.2*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Update sources" FontWeight="SemiBold" Margin="0,0,0,8" />
                                <ItemsControl ItemsSource="{x:Bind ProviderOptions}">
                                    <ItemsControl.ItemsPanel><ItemsPanelTemplate><ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="2" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Content="{Binding DisplayName}" MinWidth="190" Margin="0,0,8,4" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,4,0,0">
                                    <Button Content="Policy only" Tag="Policy" Click="ProviderShortcut_Click" />
                                    <Button Content="Microsoft sources" Tag="Microsoft" Click="ProviderShortcut_Click" />
                                    <Button Content="All" Tag="All" Click="ProviderShortcut_Click" />
                                    <Button Content="Clear" Tag="Clear" Click="ProviderShortcut_Click" />
                                </StackPanel>
                                <TextBox x:Name="CustomServiceIdBox" Header="Custom service ID (optional)" PlaceholderText="WUA service GUID" Width="360" HorizontalAlignment="Left" Margin="0,8,0,0" />
                                <TextBox x:Name="OfflineCabPathBox" Header="Offline catalog (optional)" PlaceholderText="C:\support\Wsusscn2.cab — security metadata only; no drivers" Width="520" HorizontalAlignment="Left" Margin="0,8,0,0" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Spacing="8">
                                <TextBlock Text="Saved scan profiles" FontWeight="SemiBold" />
                                <ComboBox x:Name="SavedProfileCombo" ItemsSource="{x:Bind SavedProfiles}" DisplayMemberPath="Name" PlaceholderText="Choose a saved profile" HorizontalAlignment="Stretch" />
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <Button Content="Apply" Click="ApplySavedProfile_Click" />
                                    <Button Content="Save current" Click="SaveProfile_Click" />
                                    <Button Content="Delete" Click="DeleteProfile_Click" />
                                </StackPanel>
                                <TextBlock Text="Search profile" FontWeight="SemiBold" Margin="0,4,0,0" />
                                <ComboBox x:Name="PresetCombo" ItemsSource="{x:Bind PresetOptions}" DisplayMemberPath="DisplayName" SelectedIndex="2" SelectionChanged="PresetCombo_SelectionChanged" HorizontalAlignment="Stretch" />
                                <TextBox x:Name="CustomCriteriaBox" Header="Custom WUA criteria" PlaceholderText="IsInstalled=0 and Type='Driver'" Visibility="Collapsed" />
                                <CheckBox x:Name="SupersededCheck" Content="Include potentially superseded updates" />
                            </StackPanel>
                            <StackPanel Grid.Column="2" VerticalAlignment="Bottom" Spacing="8">
                                <Button x:Name="ScanButton" Content="Start scan" Style="{StaticResource AccentButtonStyle}" Click="Scan_Click" MinWidth="120" />
                                <Button x:Name="CancelButton" Content="Cancel" Click="Cancel_Click" IsEnabled="False" MinWidth="120" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border x:Name="ScanInsightCard" Grid.Row="2" Style="{StaticResource SectionCardStyle}" Margin="0,0,0,16" Visibility="Collapsed">
                        <Grid ColumnSpacing="16" RowSpacing="10">
                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel Spacing="4">
                                <TextBlock Text="Scan insights" FontWeight="SemiBold" />
                                <TextBlock x:Name="InsightCountsText" TextWrapping="Wrap" />
                                <TextBlock x:Name="InsightSizeText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Spacing="4">
                                <TextBlock Text="Safety and source health" FontWeight="SemiBold" />
                                <TextBlock x:Name="InsightSafetyText" TextWrapping="Wrap" />
                                <TextBlock x:Name="ComparisonPreviewText" Foreground="{StaticResource WuPilotAccentBrush}" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Grid.Column="2" Spacing="8" VerticalAlignment="Center">
                                <Button x:Name="ViewChangesButton" Content="View comparison" Click="ShowComparison_Click" IsEnabled="False" />
                                <Button x:Name="RetryFailedButton" Content="Retry failed sources" Click="RetryFailedSources_Click" IsEnabled="False" />
                            </StackPanel>
                            <TextBox x:Name="TechnicianNotesBox" Grid.Row="1" Grid.ColumnSpan="3" Header="Technician notes included with evidence exports" PlaceholderText="Add ticket context, observed behavior, or follow-up guidance…" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="56" MaxHeight="110" />
                        </Grid>
                    </Border>

                    <Grid Grid.Row="3" ColumnSpacing="16">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="3*" MinWidth="460" /><ColumnDefinition Width="2*" MinWidth="330" /></Grid.ColumnDefinitions>
                        <Border Style="{StaticResource SectionCardStyle}" Padding="0">
                            <Grid>
                                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                                <Grid Padding="16,12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" RowSpacing="8">
                                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" Spacing="12">
                                        <TextBlock Text="Results" FontWeight="SemiBold" />
                                        <TextBlock Text="{x:Bind ResultSummary, Mode=OneWay}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        <TextBlock x:Name="VisibleResultCountText" Text="0 shown" Foreground="{StaticResource WuPilotAccentBrush}" />
                                        <TextBlock x:Name="ResultsStateText" Text="Run a scan to load results." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6">
                                        <Button Content="Select visible" Click="SelectVisible_Click" Padding="10,4" />
                                        <Button Content="Clear selection" Click="ClearSelection_Click" Padding="10,4" />
                                        <Button Content="Reset filters" Click="ClearResultFilters_Click" Padding="10,4" />
                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8">
                                        <TextBox x:Name="FilterBox" Width="240" PlaceholderText="Title, source, KB, or CVE" TextChanged="FilterBox_TextChanged" />
                                        <ComboBox x:Name="ResultFilterCombo" Width="150" ItemsSource="{x:Bind ResultFilterOptions}" DisplayMemberPath="DisplayName" SelectedIndex="0" SelectionChanged="ResultFilter_SelectionChanged" />
                                        <ComboBox x:Name="ResultSortCombo" Width="190" ItemsSource="{x:Bind ResultSortOptions}" DisplayMemberPath="DisplayName" SelectedIndex="0" SelectionChanged="ResultSort_SelectionChanged" />
                                    </StackPanel>
                                    <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8">
                                        <Button x:Name="InstallAllButton" Content="Install all" Click="InstallAll_Click" Style="{StaticResource AccentButtonStyle}" IsEnabled="False" ToolTipService.ToolTip="Installs all eligible updates sequentially, including updates that may display a prompt." />
                                        <Button x:Name="InstallSelectedButton" Content="Install selected" Click="InstallSelected_Click" IsEnabled="False" ToolTipService.ToolTip="Select one or more missing updates to enable this action." />
                                        <Button x:Name="DownloadSelectedButton" Content="Download selected" Click="DownloadSelected_Click" IsEnabled="False" ToolTipService.ToolTip="Select one or more missing updates to download their payloads." />
                                        <TextBlock x:Name="SelectedResultCountText" Text="0 selected" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    </StackPanel>
                                </Grid>
                                <ListView x:Name="UpdatesList" Grid.Row="2" ItemsSource="{x:Bind VisibleUpdates}" SelectionChanged="UpdatesList_SelectionChanged" SelectionMode="Multiple"
                                          ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollMode="Enabled">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Padding="8" MinHeight="74">
                                                <Grid.ColumnDefinitions><ColumnDefinition Width="36" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                                <FontIcon Glyph="&#xE777;" FontSize="16" VerticalAlignment="Top" Margin="0,4,0,0" />
                                                <StackPanel Grid.Column="1" Spacing="3">
                                                    <TextBlock Text="{Binding Title}" TextWrapping="Wrap" FontWeight="SemiBold" MaxLines="2" />
                                                    <TextBlock Text="{Binding Metadata}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
                                                    <TextBlock Text="{Binding SourceLabel}" Foreground="{StaticResource WuPilotAccentBrush}" FontSize="12" />
                                                    <TextBlock Text="{Binding InstallationCapabilityLabel}" Foreground="{ThemeResource SystemFillColorCautionBrush}" FontSize="12" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="2" HorizontalAlignment="Right" Spacing="4">
                                                    <Border Padding="6,2" CornerRadius="10" Background="{ThemeResource ControlFillColorSecondaryBrush}"><TextBlock Text="{Binding TypeLabel}" FontSize="11" /></Border>
                                                    <TextBlock Text="{Binding SizeLabel}" FontSize="11" HorizontalAlignment="Right" />
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </Grid>
                        </Border>

                        <Border Grid.Column="1" Style="{StaticResource SectionCardStyle}">
                            <ScrollViewer>
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Update details" FontWeight="SemiBold" FontSize="16" />
                                    <TextBlock x:Name="DetailTitle" Text="Select an update to inspect its evidence." TextWrapping="Wrap" FontSize="18" FontWeight="SemiBold" />
                                    <TextBlock x:Name="DetailDescription" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" MaxLines="6" />
                                    <Grid ColumnSpacing="12" RowSpacing="8">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="110" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                                        <TextBlock Text="Identity" FontWeight="SemiBold" /><TextBlock x:Name="DetailIdentity" Grid.Column="1" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" />
                                        <TextBlock Grid.Row="1" Text="Sources" FontWeight="SemiBold" /><TextBlock x:Name="DetailSources" Grid.Row="1" Grid.Column="1" TextWrapping="Wrap" />
                                        <TextBlock Grid.Row="2" Text="Manufacturer" FontWeight="SemiBold" /><TextBlock x:Name="DetailManufacturer" Grid.Row="2" Grid.Column="1" TextWrapping="Wrap" />
                                        <TextBlock Grid.Row="3" Text="Model / class" FontWeight="SemiBold" /><TextBlock x:Name="DetailModel" Grid.Row="3" Grid.Column="1" TextWrapping="Wrap" />
                                        <TextBlock Grid.Row="4" Text="Hardware ID" FontWeight="SemiBold" /><TextBlock x:Name="DetailHardware" Grid.Row="4" Grid.Column="1" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" />
                                        <TextBlock Grid.Row="5" Text="Version / date" FontWeight="SemiBold" /><TextBlock x:Name="DetailDate" Grid.Row="5" Grid.Column="1" />
                                        <TextBlock Grid.Row="6" Text="Installed now" FontWeight="SemiBold" /><TextBlock x:Name="DetailInstalled" Grid.Row="6" Grid.Column="1" TextWrapping="Wrap" />
                                        <TextBlock Grid.Row="7" Text="Signature / match" FontWeight="SemiBold" /><TextBlock x:Name="DetailSignature" Grid.Row="7" Grid.Column="1" TextWrapping="Wrap" />
                                        <TextBlock Grid.Row="8" Text="Categories" FontWeight="SemiBold" /><TextBlock x:Name="DetailCategories" Grid.Row="8" Grid.Column="1" TextWrapping="Wrap" />
                                    </Grid>
                                    <InfoBar x:Name="DetailWarning" Severity="Warning" IsOpen="False" IsClosable="False" Title="Review before installation" Message="Direct installation tests this device only. Use the evidence bundle and Intune deployment rings for broad rollout." />
                                    <StackPanel Spacing="8">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <Button x:Name="CopyDetailsButton" Content="Copy details" Click="CopyDetails_Click" IsEnabled="False" />
                                            <Button x:Name="OpenSupportButton" Content="Support page" Click="OpenSupport_Click" IsEnabled="False" />
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <Button x:Name="WatchUpdateButton" Content="Add to watchlist" Click="ToggleWatch_Click" IsEnabled="False" />
                                            <Button x:Name="ExportSelectedButton" Content="Export selected" Click="ExportSelected_Click" IsEnabled="False" />
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <Button x:Name="DownloadButton" Content="Download" Click="UpdateAction_Click" Tag="Download" IsEnabled="False" />
                                            <Button x:Name="InstallButton" Content="Install" Click="UpdateAction_Click" Tag="Install" IsEnabled="False" />
                                            <Button x:Name="HideButton" Content="Hide" Click="UpdateAction_Click" Tag="Hide" IsEnabled="False" />
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </Grid>

                <Grid x:Name="CompareView" Visibility="Collapsed" Padding="24,16,24,20">
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Compare scans" Style="{StaticResource TitleTextBlockStyle}" />
                            <TextBlock Text="See what changed between the two most recent scans in this session." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <Button Grid.Column="1" Content="Return to scan" Click="ReturnToScan_Click" VerticalAlignment="Center" />
                    </Grid>
                    <Border Grid.Row="1" Style="{StaticResource SectionCardStyle}" Margin="0,16">
                        <StackPanel Spacing="6">
                            <TextBlock x:Name="ComparisonSummaryText" Text="Run two scans to compare results." FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
                            <TextBlock x:Name="ComparisonContextText" Text="Comparison is session-only and does not change update state." Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="2" Style="{StaticResource SectionCardStyle}" Padding="0">
                        <Grid>
                            <TextBlock x:Name="ComparisonEmptyText" Text="No comparison changes to display." HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <ListView ItemsSource="{x:Bind ScanChanges}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="14,10" ColumnSpacing="12">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="130" /><ColumnDefinition Width="*" /><ColumnDefinition Width="220" /></Grid.ColumnDefinitions>
                                            <Border Padding="8,3" CornerRadius="12" Background="{ThemeResource ControlFillColorSecondaryBrush}" HorizontalAlignment="Left" VerticalAlignment="Top">
                                                <TextBlock Text="{Binding KindLabel}" FontSize="12" />
                                            </Border>
                                            <StackPanel Grid.Column="1" Spacing="3">
                                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                <TextBlock Text="{Binding Summary}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <TextBlock Grid.Column="2" Text="{Binding Identity}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" HorizontalAlignment="Right" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Border>
                </Grid>

                <Grid x:Name="WatchlistView" Visibility="Collapsed" Padding="24,16,24,20">
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Update watchlist" Style="{StaticResource TitleTextBlockStyle}" />
                            <TextBlock Text="Track important updates across scans without downloading, installing, or approving them." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBlock x:Name="WatchlistSummaryText" Text="0 watched" Foreground="{StaticResource WuPilotAccentBrush}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Button Content="Copy watchlist" Click="CopyWatchlist_Click" />
                            <Button Content="Remove selected" Click="RemoveWatched_Click" />
                        </StackPanel>
                    </Grid>
                    <InfoBar Grid.Row="1" Margin="0,16" IsOpen="True" IsClosable="False" Severity="Informational" Title="Tracking only" Message="The watchlist stores last-known metadata locally. It refreshes after each scan and never queues an update action." />
                    <Border Grid.Row="2" Style="{StaticResource SectionCardStyle}" Padding="0">
                        <ListView x:Name="WatchlistList" ItemsSource="{x:Bind WatchedUpdates}" SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="14,10" ColumnSpacing="14">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="250" /><ColumnDefinition Width="210" /></Grid.ColumnDefinitions>
                                        <StackPanel Spacing="3">
                                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            <TextBlock Text="{Binding Identity}" FontFamily="Consolas" FontSize="11" />
                                            <TextBlock Text="{Binding Sources}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Spacing="3">
                                            <TextBlock Text="{Binding StatusLabel}" Foreground="{StaticResource WuPilotAccentBrush}" />
                                            <TextBlock Text="{Binding StateLabel}" TextWrapping="Wrap" FontSize="12" />
                                            <TextBlock Text="{Binding DriverLabel}" TextWrapping="Wrap" FontSize="12" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                            <Border Padding="8,3" CornerRadius="12" Background="{ThemeResource ControlFillColorSecondaryBrush}" HorizontalAlignment="Right"><TextBlock Text="{Binding TypeLabel}" FontSize="11" /></Border>
                                            <TextBlock Text="{Binding CheckedLabel}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="11" Margin="0,6,0,0" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </Grid>

                <Grid x:Name="SourcesView" Visibility="Collapsed" Padding="24,16,24,20">
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Registered update sources" Style="{StaticResource TitleTextBlockStyle}" />
                            <TextBlock Text="Inspect Windows Update Agent service registrations without changing them." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBlock x:Name="RegisteredSourcesSummaryText" Text="Refresh to inventory WUA service registrations." Foreground="{StaticResource WuPilotAccentBrush}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Button Content="Copy inventory" Click="CopySources_Click" />
                            <Button Content="Use selected for scan" Click="UseSourceForScan_Click" />
                            <Button Content="Refresh sources" Click="RefreshSources_Click" Style="{StaticResource AccentButtonStyle}" />
                        </StackPanel>
                    </Grid>
                    <InfoBar Grid.Row="1" Margin="0,16" IsOpen="True" IsClosable="False" Severity="Informational" Title="Read-only inventory" Message="WuPilot lists existing registrations only. Selecting a source fills the custom scan field; it does not call AddService2 or register the device for a new service." />
                    <Border Grid.Row="2" Style="{StaticResource SectionCardStyle}" Padding="0">
                        <ListView x:Name="RegisteredSourcesList" ItemsSource="{x:Bind RegisteredSources}" SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="14,10" ColumnSpacing="14">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="240" /><ColumnDefinition Width="*" /><ColumnDefinition Width="260" /></Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                            <TextBlock Text="{Binding CapabilityLabel}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{Binding ServiceId}" FontFamily="Consolas" TextWrapping="Wrap" VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="2" Text="{Binding RoleLabel}" Foreground="{StaticResource WuPilotAccentBrush}" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </Grid>

                <Grid x:Name="ControlsView" Visibility="Collapsed" Padding="24,16,24,20">
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Windows Update controls" Style="{StaticResource TitleTextBlockStyle}" />
                            <TextBlock Text="Inspect requested and effective policy, then make audited, reversible device-local changes." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBlock x:Name="ControlsSummaryText" Text="Refresh to read Windows Update and Delivery Optimization policy." Foreground="{StaticResource WuPilotAccentBrush}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Button Content="Windows Settings" Click="OpenWindowsUpdate_Click" />
                            <Button Content="Export audit" Click="ExportSettingsAudit_Click" />
                            <Button Content="Refresh" Click="RefreshSettings_Click" Style="{StaticResource AccentButtonStyle}" />
                        </StackPanel>
                    </Grid>
                    <Border Grid.Row="1" Style="{StaticResource SectionCardStyle}" Margin="0,16">
                        <StackPanel Spacing="10">
                            <TextBlock Text="Quick controls" FontWeight="SemiBold" />
                            <TextBlock Text="Managed values can be requested locally after confirmation, but Group Policy or MDM may restore the effective value." Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button Content="Stage Microsoft products" Tag="update.microsoft-products" Click="QuickToggle_Click" />
                                <Button Content="Stage latest updates" Tag="update.latest" Click="QuickToggle_Click" />
                                <Button Content="Stage metered downloads" Tag="update.metered" Click="QuickToggle_Click" />
                                <Button Content="Stage restart notices" Tag="update.restart-notifications" Click="QuickToggle_Click" />
                                <Button Content="Stage pause today" Click="PauseUpdates_Click" />
                                <Button Content="Stage resume" Click="ResumeUpdates_Click" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Grid Grid.Row="2" ColumnSpacing="16">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="1.25*" /><ColumnDefinition Width="0.9*" /></Grid.ColumnDefinitions>
                        <Border Style="{StaticResource SectionCardStyle}">
                            <Grid RowSpacing="10">
                                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBox x:Name="PolicyFilterBox" Width="300" PlaceholderText="Search policy, category, or value" TextChanged="PolicyFilter_Changed" />
                                    <ComboBox x:Name="PolicyCategoryCombo" Width="160" PlaceholderText="All categories" SelectionChanged="PolicyFilterCombo_Changed" />
                                    <ComboBox x:Name="PolicyOwnershipCombo" Width="130" SelectedIndex="0" SelectionChanged="PolicyFilterCombo_Changed"><ComboBoxItem Content="All owners" Tag="All" /><ComboBoxItem Content="Unconfigured" Tag="Unconfigured" /><ComboBoxItem Content="Group Policy" Tag="GroupPolicy" /><ComboBoxItem Content="MDM" Tag="Mdm" /><ComboBoxItem Content="Windows UX" Tag="WindowsUx" /></ComboBox>
                                    <ComboBox x:Name="PolicyRiskCombo" Width="115" SelectedIndex="0" SelectionChanged="PolicyFilterCombo_Changed"><ComboBoxItem Content="All risks" Tag="All" /><ComboBoxItem Content="Normal" Tag="Normal" /><ComboBoxItem Content="Elevated" Tag="Elevated" /><ComboBoxItem Content="High" Tag="High" /></ComboBox>
                                </StackPanel>
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                                    <ComboBox x:Name="PolicyStateFilterCombo" Width="170" SelectedIndex="0" SelectionChanged="PolicyFilterCombo_Changed"><ComboBoxItem Content="All states" Tag="All" /><ComboBoxItem Content="Editable" Tag="Editable" /><ComboBoxItem Content="View only" Tag="ViewOnly" /><ComboBoxItem Content="Requested ≠ effective" Tag="Different" /><ComboBoxItem Content="Favorites" Tag="Favorites" /></ComboBox>
                                    <CheckBox x:Name="ShowLegacyPolicyCheck" Content="Show legacy / unsupported" Checked="PolicyFilter_Changed" Unchecked="PolicyFilter_Changed" VerticalAlignment="Center" />
                                    <TextBlock x:Name="VisiblePolicyCountText" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                </StackPanel>
                                <ListView x:Name="PolicyList" Grid.Row="2" ItemsSource="{x:Bind VisiblePolicies}" SelectionChanged="PolicyList_SelectionChanged">
                                    <ListView.ItemTemplate><DataTemplate><Grid Padding="8" ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="36" /><ColumnDefinition Width="*" /><ColumnDefinition Width="180" /></Grid.ColumnDefinitions><Button Content="{Binding FavoriteGlyph}" Tag="{Binding}" Click="FavoritePolicy_Click" ToolTipService.ToolTip="Add or remove favorite" /><StackPanel Grid.Column="1" Spacing="2"><TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextWrapping="Wrap" /><TextBlock Text="{Binding Description}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" /><TextBlock Text="{Binding Category}" FontSize="11" Foreground="{StaticResource WuPilotAccentBrush}" /></StackPanel><StackPanel Grid.Column="2" HorizontalAlignment="Right"><TextBlock Text="{Binding ValueLabel}" FontWeight="SemiBold" HorizontalAlignment="Right" TextWrapping="Wrap" /><TextBlock Text="{Binding OwnershipLabel}" FontSize="11" HorizontalAlignment="Right" /><TextBlock Text="{Binding EditLabel}" FontSize="11" HorizontalAlignment="Right" /></StackPanel></Grid></DataTemplate></ListView.ItemTemplate>
                                </ListView>
                            </Grid>
                        </Border>
                        <Border Grid.Column="1" Style="{StaticResource SectionCardStyle}">
                            <ScrollViewer><StackPanel Spacing="10">
                                <TextBlock Text="Selected policy" FontWeight="SemiBold" />
                                <TextBlock x:Name="PolicyDetailTitle" Text="Select a policy." FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
                                <TextBlock x:Name="PolicyDetailStatus" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <ToggleSwitch x:Name="PolicyBooleanEditor" Header="Requested value" OnContent="On" OffContent="Off" Visibility="Collapsed" />
                                <ComboBox x:Name="PolicyChoiceEditor" Header="Requested value" ItemsSource="{x:Bind PolicyChoices}" DisplayMemberPath="DisplayName" Visibility="Collapsed" />
                                <NumberBox x:Name="PolicyNumberEditor" Header="Requested value" SpinButtonPlacementMode="Compact" Visibility="Collapsed" />
                                <CalendarDatePicker x:Name="PolicyDateEditor" Header="Requested value" Visibility="Collapsed" />
                                <TextBox x:Name="PolicyValueBox" Header="Requested value" PlaceholderText="Enter the documented value" Visibility="Collapsed" />
                                <InfoBar x:Name="PolicyValidationInfo" IsOpen="False" IsClosable="False" Severity="Error" Title="Value needs attention" />
                                <HyperlinkButton x:Name="PolicyDocumentationLink" Content="Open Microsoft policy documentation" Visibility="Collapsed" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Button x:Name="ApplyPolicyButton" Content="Stage change" Click="ApplyPolicy_Click" IsEnabled="False" Style="{StaticResource AccentButtonStyle}" />
                                    <Button x:Name="ClearPolicyButton" Content="Stage Windows default" Click="ClearPolicy_Click" IsEnabled="False" />
                                </StackPanel>
                                <TextBlock Text="Pending change cart" FontWeight="SemiBold" Margin="0,12,0,0" />
                                <ListView x:Name="PolicyChangeCartList" ItemsSource="{x:Bind PolicyChangeCart}" MaxHeight="220">
                                    <ListView.ItemTemplate><DataTemplate><StackPanel Padding="4"><TextBlock Text="{Binding Title}" FontWeight="SemiBold" /><TextBlock Text="{Binding Summary}" FontSize="11" TextWrapping="Wrap" /><TextBlock Text="{Binding Warning}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" /></StackPanel></DataTemplate></ListView.ItemTemplate>
                                </ListView>
                                <StackPanel Orientation="Horizontal" Spacing="8"><Button Content="Apply cart and verify" Click="ApplyPolicyCart_Click" Style="{StaticResource AccentButtonStyle}" /><Button Content="Remove selected" Click="RemovePolicyCartItem_Click" /><Button Content="Clear cart" Click="ClearPolicyCart_Click" /></StackPanel>
                                <TextBlock Text="Recent changes" FontWeight="SemiBold" Margin="0,12,0,0" />
                                <ListView x:Name="SettingsAuditList" ItemsSource="{x:Bind SettingsAudit}" MaxHeight="260">
                                    <ListView.ItemTemplate><DataTemplate><StackPanel Padding="4"><TextBlock Text="{Binding Title}" FontWeight="SemiBold" /><TextBlock Text="{Binding Summary}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" /></StackPanel></DataTemplate></ListView.ItemTemplate>
                                </ListView>
                                <StackPanel Orientation="Horizontal" Spacing="8"><Button Content="Restore selected change" Click="RestoreSetting_Click" /><Button Content="Copy audit details" Click="CopyAuditDetails_Click" /></StackPanel>
                            </StackPanel></ScrollViewer>
                        </Border>
                    </Grid>
                </Grid>

                <Grid x:Name="PerformanceView" Visibility="Collapsed" Padding="24,16,24,20">
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <StackPanel><TextBlock Text="Update performance" Style="{StaticResource TitleTextBlockStyle}" /><TextBlock Text="Exact WuPilot operation timings and Delivery Optimization statistics. Reconstructed Windows history is explicitly labeled as estimated." Foreground="{ThemeResource TextFillColorSecondaryBrush}" /></StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center"><ComboBox x:Name="PerformanceRangeCombo" Width="130" SelectedIndex="1" SelectionChanged="PerformanceRange_Changed"><ComboBoxItem Content="7 days" Tag="7" /><ComboBoxItem Content="30 days" Tag="30" /><ComboBoxItem Content="90 days" Tag="90" /><ComboBoxItem Content="All history" Tag="0" /></ComboBox><Button Content="Refresh" Click="RefreshPerformance_Click" Style="{StaticResource AccentButtonStyle}" /></StackPanel>
                    </Grid>
                    <Grid Grid.Row="1" Margin="0,16" ColumnSpacing="12">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <Border Style="{StaticResource SectionCardStyle}"><StackPanel><TextBlock Text="Downloaded" Foreground="{ThemeResource TextFillColorSecondaryBrush}" /><TextBlock x:Name="DoDownloadedText" Text="—" FontSize="22" FontWeight="SemiBold" /></StackPanel></Border>
                        <Border Grid.Column="1" Style="{StaticResource SectionCardStyle}"><StackPanel><TextBlock Text="Peer / cache savings" Foreground="{ThemeResource TextFillColorSecondaryBrush}" /><TextBlock x:Name="DoSavingsText" Text="—" FontSize="22" FontWeight="SemiBold" /></StackPanel></Border>
                        <Border Grid.Column="2" Style="{StaticResource SectionCardStyle}"><StackPanel><TextBlock Text="Uploaded" Foreground="{ThemeResource TextFillColorSecondaryBrush}" /><TextBlock x:Name="DoUploadedText" Text="—" FontSize="22" FontWeight="SemiBold" /></StackPanel></Border>
                        <Border Grid.Column="3" Style="{StaticResource SectionCardStyle}"><StackPanel><TextBlock Text="Mode / active" Foreground="{ThemeResource TextFillColorSecondaryBrush}" /><TextBlock x:Name="DoModeText" Text="—" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" /></StackPanel></Border>
                    </Grid>
                    <Grid Grid.Row="2" ColumnSpacing="16">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="1.25*" /><ColumnDefinition Width="0.75*" /></Grid.ColumnDefinitions>
                        <Border Style="{StaticResource SectionCardStyle}">
                            <Grid><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions><TextBlock x:Name="PerformanceSummaryText" Text="No retained operations." FontWeight="SemiBold" /><ListView x:Name="PerformanceList" Grid.Row="1" ItemsSource="{x:Bind VisibleMetrics}" SelectionMode="Single" SelectionChanged="PerformanceList_SelectionChanged"><ListView.ItemTemplate><DataTemplate><Grid Padding="8" ColumnSpacing="12"><Grid.ColumnDefinitions><ColumnDefinition Width="150" /><ColumnDefinition Width="*" /><ColumnDefinition Width="200" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{Binding When}" /><TextBlock Text="{Binding Result}" FontSize="11" /></StackPanel><TextBlock Grid.Column="1" Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap" /><TextBlock Grid.Column="2" Text="{Binding Timing}" FontSize="11" TextWrapping="Wrap" HorizontalAlignment="Right" /></Grid></DataTemplate></ListView.ItemTemplate></ListView></Grid>
                        </Border>
                        <Border Grid.Column="1" Style="{StaticResource SectionCardStyle}">
                            <ScrollViewer>
                                <StackPanel Spacing="8">
                                    <TextBlock Text="Operation details" FontWeight="SemiBold" />
                                    <TextBlock x:Name="OperationDetailTitle" Text="Select an operation to inspect its diagnostics." FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" />
                                    <TextBlock x:Name="OperationDetailText" Text="No operation selected." TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    <InfoBar x:Name="OperationRecommendation" IsOpen="False" IsClosable="False" Severity="Warning" Title="Recommended next action" />
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Button x:Name="CopyOperationButton" Content="Copy details" Click="CopyOperationDetails_Click" IsEnabled="False" />
                                        <Button Content="Export evidence" Click="Export_Click" IsEnabled="{x:Bind HasScanReport, Mode=OneWay}" />
                                        <Button Content="Open Windows Update" Click="OpenWindowsUpdate_Click" />
                                    </StackPanel>
                                    <TextBlock Text="Delivery Optimization evidence" FontWeight="SemiBold" Margin="0,12,0,0" />
                                    <TextBlock x:Name="DoDetailText" Text="Refresh to collect supported Delivery Optimization telemetry." TextWrapping="Wrap" />
                                    <InfoBar IsOpen="True" IsClosable="False" Severity="Informational" Title="Timing confidence" Message="WuPilot actions use monotonic timers. Windows history and reboot correlations are shown only when matching event boundaries exist and are labeled estimated." />
                                </StackPanel>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </Grid>

                <Grid x:Name="DiagnosticsView" Visibility="Collapsed" Padding="24,16,24,20">
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Diagnostics and repair" Style="{StaticResource TitleTextBlockStyle}" />
                            <TextBlock Text="Read local policy, service, proxy, DNS, WUA, join, and reboot state before changing the device." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBlock x:Name="DiagnosticSummaryText" Text="Run checks to build a diagnostic summary." Foreground="{StaticResource WuPilotAccentBrush}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Button Content="Windows Update settings" Click="OpenWindowsUpdate_Click" />
                            <Button Content="Copy summary" Click="CopyDiagnostics_Click" />
                            <Button Content="Refresh checks" Click="Diagnostics_Click" Style="{StaticResource AccentButtonStyle}" />
                        </StackPanel>
                    </Grid>
                    <Border Grid.Row="1" Style="{StaticResource SectionCardStyle}" Margin="0,16">
                        <StackPanel Spacing="10">
                            <TextBlock Text="Bounded repair actions" FontWeight="SemiBold" />
                            <TextBlock Text="Each action shows a confirmation. Reset cache renames stores to timestamped recovery paths; it never deletes them." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button Content="Start required services" Tag="StartRequiredServices" Click="Repair_Click" />
                                <Button Content="Reset update cache" Tag="ResetWindowsUpdateCache" Click="Repair_Click" />
                                <Button Content="DISM ScanHealth" Tag="ScanComponentStore" Click="Repair_Click" />
                                <Button Content="DISM RestoreHealth" Tag="RestoreComponentStore" Click="Repair_Click" />
                                <Button Content="Generate WindowsUpdate.log" Tag="GenerateWindowsUpdateLog" Click="Repair_Click" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Grid Grid.Row="2" ColumnSpacing="16">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="1.2*" /><ColumnDefinition Width="1*" /></Grid.ColumnDefinitions>
                        <Border Style="{StaticResource SectionCardStyle}">
                            <Grid RowSpacing="8">
                                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="Findings" FontWeight="SemiBold" VerticalAlignment="Center" />
                                    <TextBox x:Name="DiagnosticFilterBox" Width="220" PlaceholderText="Filter findings" TextChanged="DiagnosticFilter_Changed" />
                                    <ComboBox x:Name="DiagnosticSeverityCombo" Width="150" ItemsSource="{x:Bind DiagnosticSeverityOptions}" DisplayMemberPath="DisplayName" SelectedIndex="0" SelectionChanged="DiagnosticSeverity_SelectionChanged" />
                                </StackPanel>
                                <ListView Grid.Row="1" ItemsSource="{x:Bind DiagnosticFindings}">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Padding="4,8" Spacing="3">
                                                <StackPanel Orientation="Horizontal" Spacing="8">
                                                    <Border Padding="7,2" CornerRadius="10" Background="{ThemeResource ControlFillColorSecondaryBrush}"><TextBlock Text="{Binding SeverityLabel}" FontSize="11" /></Border>
                                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                                </StackPanel>
                                                <TextBlock Text="{Binding Summary}" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                                <TextBlock Text="{Binding Recommendation}" TextWrapping="Wrap" Foreground="{StaticResource WuPilotAccentBrush}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </Grid>
                        </Border>
                        <Border Grid.Column="1" Style="{StaticResource SectionCardStyle}"><ScrollViewer><StackPanel Spacing="8"><TextBlock Text="Recent update history" FontWeight="SemiBold" /><ItemsControl ItemsSource="{x:Bind UpdateHistory}"><ItemsControl.ItemTemplate><DataTemplate><Grid Padding="0,4" ColumnSpacing="8"><Grid.ColumnDefinitions><ColumnDefinition Width="110" /><ColumnDefinition Width="*" /><ColumnDefinition Width="42" /></Grid.ColumnDefinitions><TextBlock Text="{Binding Date}" FontSize="11" /><TextBlock Grid.Column="1" Text="{Binding Title}" TextTrimming="CharacterEllipsis" /><TextBlock Grid.Column="2" Text="{Binding ResultCode}" FontFamily="Consolas" HorizontalAlignment="Right" /></Grid></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><TextBlock Text="Service state" FontWeight="SemiBold" Margin="0,12,0,0" /><ItemsControl ItemsSource="{x:Bind ServiceStates}"><ItemsControl.ItemTemplate><DataTemplate><Grid Padding="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="120" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><TextBlock Text="{Binding Key}" FontFamily="Consolas" /><TextBlock Grid.Column="1" Text="{Binding Value}" /></Grid></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><TextBlock Text="Policy evidence" FontWeight="SemiBold" Margin="0,12,0,0" /><ItemsControl ItemsSource="{x:Bind PolicyStates}"><ItemsControl.ItemTemplate><DataTemplate><StackPanel Padding="0,4"><TextBlock Text="{Binding Key}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="11" /><TextBlock Text="{Binding Value}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" /></StackPanel></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel></ScrollViewer></Border>
                    </Grid>
                </Grid>

                <Grid x:Name="HistoryView" Visibility="Collapsed" Padding="24,16,24,20">
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Update history" Style="{StaticResource TitleTextBlockStyle}" />
                            <TextBlock Text="Explore recent Windows Update Agent operations without starting a new scan." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBlock x:Name="HistorySummaryText" Text="Refresh to load up to 500 recent history events." Foreground="{StaticResource WuPilotAccentBrush}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Button Content="Copy visible" Click="CopyHistory_Click" />
                            <Button Content="Refresh history" Click="RefreshHistory_Click" Style="{StaticResource AccentButtonStyle}" />
                        </StackPanel>
                    </Grid>
                    <Border Grid.Row="1" Style="{StaticResource SectionCardStyle}" Margin="0,16">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <TextBox x:Name="HistoryFilterBox" Width="320" PlaceholderText="Title, HRESULT, source, or Update ID" TextChanged="HistoryFilter_Changed" />
                            <CheckBox x:Name="HistoryFailuresOnlyCheck" Content="Failures and partial results only" Checked="HistoryFilter_Changed" Unchecked="HistoryFilter_Changed" VerticalAlignment="Center" />
                            <TextBlock x:Name="VisibleHistoryCountText" Text="0 shown" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="2" Style="{StaticResource SectionCardStyle}" Padding="0">
                        <ListView ItemsSource="{x:Bind VisibleUpdateHistory}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="14,9" ColumnSpacing="12">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="150" /><ColumnDefinition Width="150" /><ColumnDefinition Width="*" /><ColumnDefinition Width="130" /></Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding DateLabel}" FontSize="12" />
                                            <TextBlock Text="{Binding OperationLabel}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="11" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding ResultLabel}" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding HResultLabel}" FontFamily="Consolas" FontSize="12" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" Spacing="2">
                                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap" MaxLines="2" />
                                            <TextBlock Text="{Binding SourceLabel}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="11" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="3" Text="{Binding UpdateIdLabel}" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap" HorizontalAlignment="Right" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </Grid>

                <Grid x:Name="ActivityView" Visibility="Collapsed" Padding="24,16,24,20">
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                        <StackPanel><TextBlock Text="Activity" Style="{StaticResource TitleTextBlockStyle}" /><TextBlock Text="Actions and results from this session. Exported evidence is the durable record." Foreground="{ThemeResource TextFillColorSecondaryBrush}" /></StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Button Content="Copy activity" Click="CopyActivity_Click" />
                            <Button Content="Clear view" Click="ClearActivity_Click" />
                        </StackPanel>
                    </Grid>
                    <Border Grid.Row="1" Style="{StaticResource SectionCardStyle}" Margin="0,16,0,0"><ListView ItemsSource="{x:Bind ActivityItems}"><ListView.ItemTemplate><DataTemplate><TextBlock Text="{Binding}" FontFamily="Consolas" TextWrapping="Wrap" Margin="0,4" /></DataTemplate></ListView.ItemTemplate></ListView></Border>
                </Grid>

                <Grid x:Name="AboutView" Visibility="Collapsed" Padding="24,16,24,20">
                    <StackPanel MaxWidth="720" HorizontalAlignment="Left" Spacing="14">
                        <TextBlock Text="About WuPilot" Style="{StaticResource TitleTextBlockStyle}" />
                        <TextBlock x:Name="AppVersionText" Foreground="{StaticResource WuPilotAccentBrush}" />
                        <TextBlock Text="WuPilot is a technician workbench for inspecting Windows Update Agent behavior, comparing scan sources, validating one-device applicability, and creating an evidence handoff for Intune administrators." TextWrapping="Wrap" />
                        <InfoBar IsOpen="True" IsClosable="False" Severity="Informational" Title="Policy-aware, not policy-breaking" Message="Direct-source scans are diagnostic comparisons. Windows policy, endpoint access, service registration, and Microsoft applicability rules still apply." />
                        <TextBlock Text="Safety model" FontWeight="SemiBold" FontSize="18" />
                        <TextBlock Text="The application requires elevation, serializes WUA operations, keeps installation user-confirmed, does not auto-reboot, and uses recoverable cache resets. Evidence bundles can contain device and tenant identifiers; handle them as support data." TextWrapping="Wrap" />
                        <TextBlock Text="Appearance" FontWeight="SemiBold" FontSize="18" />
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button Content="System theme" Tag="System" Click="Theme_Click" />
                            <Button Content="Light" Tag="Light" Click="Theme_Click" />
                            <Button Content="Dark" Tag="Dark" Click="Theme_Click" />
                        </StackPanel>
                        <ToggleSwitch x:Name="TaskbarAttentionToggle" Header="Flash the taskbar when an operation finishes while WuPilot is not focused" IsOn="True" Toggled="TaskbarAttentionToggle_Toggled" />
                        <Button Content="Reset saved layout and filters" Click="ResetPreferences_Click" HorizontalAlignment="Left" />
                        <TextBlock Text="Keyboard shortcuts" FontWeight="SemiBold" FontSize="18" />
                        <TextBlock Text="Ctrl+1 Scan · Ctrl+2 Update controls · Ctrl+3 Performance · Ctrl+F Search · F5 Refresh · Ctrl+Enter Start scan · Esc Cancel · Ctrl+Shift+E Export evidence" TextWrapping="Wrap" />
                        <TextBlock Text="Application updates" FontWeight="SemiBold" FontSize="18" />
                        <TextBlock x:Name="AppUpdateStatusText" Text="WuPilot checks for stable releases when it launches. Cached release metadata keeps the check lightweight." TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <Button Content="Check for updates" Click="CheckForUpdates_Click" HorizontalAlignment="Left" />
                        <TextBlock Text="Microsoft references" FontWeight="SemiBold" FontSize="18" />
                        <HyperlinkButton Content="WinUI 3 documentation" NavigateUri="https://learn.microsoft.com/windows/apps/winui/winui3/" />
                        <HyperlinkButton Content="Windows Update Agent API" NavigateUri="https://learn.microsoft.com/windows/win32/wua_sdk/using-the-windows-update-agent-api" />
                        <HyperlinkButton Content="Manage Windows driver updates in Intune" NavigateUri="https://learn.microsoft.com/intune/device-updates/windows/manage-driver-updates" />
                    </StackPanel>
                </Grid>
            </Grid>
        </NavigationView>

        <Grid Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="12,4">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="260" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <StackPanel VerticalAlignment="Center"><StackPanel Orientation="Horizontal" Spacing="8"><ProgressRing x:Name="BusyRing" Width="16" Height="16" IsActive="False" /><TextBlock x:Name="StatusText" Text="Ready" FontSize="12" TextTrimming="CharacterEllipsis" /></StackPanel><TextBlock x:Name="OperationOriginText" FontSize="10" Foreground="{ThemeResource TextFillColorSecondaryBrush}" /></StackPanel>
            <StackPanel Grid.Column="1" VerticalAlignment="Center"><ProgressBar x:Name="GlobalProgressBar" Height="4" IsIndeterminate="False" Value="0" /><StackPanel Orientation="Horizontal"><TextBlock x:Name="ProgressText" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" /><TextBlock x:Name="ElapsedText" FontSize="11" Margin="8,0,0,0" Foreground="{ThemeResource TextFillColorSecondaryBrush}" /></StackPanel></StackPanel>
            <Button x:Name="GlobalCancelButton" Grid.Column="2" Content="Cancel" Click="Cancel_Click" IsEnabled="False" VerticalAlignment="Center" Margin="12,0,0,0" />
        </Grid>
    </Grid>
</Window>
